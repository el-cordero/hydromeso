% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify-raster.R
\name{classify_mesohabitat_raster}
\alias{classify_mesohabitat_raster}
\title{Classify depth and velocity rasters}
\usage{
classify_mesohabitat_raster(
  depth,
  velocity = NULL,
  scheme = meso_scheme_default(),
  depth_layer = NULL,
  velocity_layer = NULL,
  align = c("error", "velocity_to_depth"),
  aoi = NULL,
  mask_aoi = TRUE,
  assume_crs = NULL,
  dry_threshold = NULL,
  invalid = c("error", "NA"),
  filename = "",
  overwrite = FALSE,
  ...
)
}
\arguments{
\item{depth}{A \code{SpatRaster} or raster path. It may contain both selected
layers when \code{velocity = NULL}.}

\item{velocity}{A separate \code{SpatRaster} or path, or \code{NULL}.}

\item{scheme}{A validated \code{meso_scheme}; values must use its units.}

\item{depth_layer, velocity_layer}{Optional layer names or positions.}

\item{align}{\code{"error"} or \code{"velocity_to_depth"}.}

\item{aoi}{Optional \code{SpatVector} or vector path.}

\item{mask_aoi}{If \code{TRUE}, crop and polygon-mask; otherwise rectangular crop.}

\item{assume_crs}{Optional CRS assigned only to inputs missing a CRS. This
does not transform coordinates and emits a message.}

\item{dry_threshold, invalid}{Classification controls.}

\item{filename}{Optional output filename.}

\item{overwrite}{Permit overwriting \code{filename}.}

\item{...}{Additional \code{\link[terra:writeRaster]{terra::writeRaster()}} options.}
}
\value{
A categorical integer \code{SpatRaster} on the (possibly AOI-cropped)
depth geometry, with a complete class table.
}
\description{
Supports separate rasters or a selected pair in one multilayer raster. Depth
is always the geometry template. Continuous velocity is aligned only when
explicitly requested and always with bilinear interpolation.
}
\examples{
d <- terra::rast(nrows = 2, ncols = 4, xmin = 0, xmax = 4, ymin = 0, ymax = 2,
                 crs = "EPSG:32615", vals = c(0.2, 0.8, 1.5, 0.2, 0.2, 0.8, 0.8, 1.5))
v <- d
terra::values(v) <- c(0.1, 0.1, 0.1, 0.4, 0.8, 0.4, 0.8, 0.8)
classify_mesohabitat_raster(d, v)
}
\seealso{
\code{\link[=classify_mesohabitat_series]{classify_mesohabitat_series()}}, \code{\link[=write_mesohabitat]{write_mesohabitat()}}
}
