% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scenarios.R
\name{classify_mesohabitat_series}
\alias{classify_mesohabitat_series}
\title{Classify paired hydraulic raster scenarios}
\usage{
classify_mesohabitat_series(
  depth,
  velocity,
  scenario_names = NULL,
  positional = FALSE,
  scheme = meso_scheme_default(),
  ...
)
}
\arguments{
\item{depth, velocity}{Multilayer \code{SpatRaster} objects, lists of rasters, or
lists of raster paths.}

\item{scenario_names}{Optional unique names that explicitly define both lists.}

\item{positional}{Permit explicit position-based pairing when names are absent.}

\item{scheme}{A \code{meso_scheme}.}

\item{...}{Passed to \code{\link[=classify_mesohabitat_raster]{classify_mesohabitat_raster()}}.}
}
\value{
A categorical multilayer \code{SpatRaster}, one layer per scenario.
}
\description{
Named scenarios are paired by unique names. Unnamed inputs are rejected
unless positional pairing is explicitly requested.
}
\examples{
d <- terra::rast(nrows = 1, ncols = 2, vals = c(0.2, 0.8), crs = "EPSG:32615")
v <- terra::rast(d); terra::values(v) <- c(0.1, 0.4)
classify_mesohabitat_series(list(low = d), list(low = v))
}
\seealso{
\code{\link[=mesohabitat_from_median_hydraulics]{mesohabitat_from_median_hydraulics()}}, \code{\link[=modal_mesohabitat]{modal_mesohabitat()}}
}
